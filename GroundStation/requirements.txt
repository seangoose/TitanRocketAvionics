# TRES Titan Ground Station — Python Dependencies
# Install on Raspberry Pi 5 with:
#   pip3 install -r requirements.txt
#
# System-level packages also required (install once):
#   sudo apt-get install python3-pyqt5 python3-pyqt5.qtwebengine
#   sudo apt-get install libgl1-mesa-glx libglib2.0-0
#   sudo apt-get install v4l-utils
#
# Pre-flight offline setup (run at home on WiFi, before competition):
#   python3 setup_offline.py

PyQt5>=5.15.0
PyQtWebEngine>=5.15.0
pyserial>=3.5
opencv-python>=4.5.0
numpy>=1.21.0
requests>=2.28.0    # used by setup_offline.py — not needed at launch site
